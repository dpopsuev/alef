name: alef-dev
description: Alef development — project-specific adapter configuration.

adapters:
  - name: shell
    blockedPatterns:
      - "--no-verify"
      - "--no-gpg-sign"
      - "git\\s+reset\\s+--hard"
      - "git\\s+commit\\s+-n"
      - "HUSKY=0"
      - "rm\\s+-rf\\s+/"
