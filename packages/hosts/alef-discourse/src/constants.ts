/** Standalone board identity. */
export const DEFAULT_BOARD_ID = "default";
/** Standalone forum identity. */
export const DEFAULT_FORUM_ID = "default";
/** Standalone actor identity when a command omits one. */
export const DEFAULT_AUTHOR_ID = "agent";
/** Bound for command list and read output. */
export const NATIVE_QUERY_LIMIT = 50;
/** Bound for queued subscription events. */
export const NATIVE_EVENT_LIMIT = 64;
/** Discourse subscription consumer identity for this adapter instance. */
export const CONSUMER_ID = "alef-adapter";
