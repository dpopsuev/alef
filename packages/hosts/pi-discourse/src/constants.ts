/** Standalone forum identity. */
export const DEFAULT_FORUM_ID = "default";
/** Standalone actor identity when a tool call omits one. */
export const DEFAULT_AUTHOR_ID = "agent";
/** Bound for native list and read output. */
export const NATIVE_QUERY_LIMIT = 50;
/** Bound for queued subscription events. */
export const NATIVE_EVENT_LIMIT = 64;
