{
	"name": "@dpopsuev/alef-organ-shell",
	"version": "0.0.1",
	"description": "Shell organ runtime primitives for command execution and process lifecycle",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"files": [
		"dist",
		"README.md"
	],
	"scripts": {
		"clean": "shx rm -rf dist",
		"build": "tsgo -p tsconfig.build.json",
		"dev": "tsgo -p tsconfig.build.json --watch --preserveWatchOutput",
		"test": "vitest --run",
		"prepublishOnly": "npm run clean && npm run build"
	},
	"keywords": [
		"agent",
		"organ",
		"shell",
		"runtime"
	],
	"author": "Mario Zechner",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/dpopsuev/alef.git",
		"directory": "packages/organ-shell"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"dependencies": {
		"@dpopsuev/alef-spine": "^0.0.1"
	},
	"devDependencies": {
		"@types/node": "^24.3.0",
		"shx": "^0.4.0",
		"typescript": "^5.9.2",
		"vitest": "^3.2.4"
	}
}
