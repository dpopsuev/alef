name: alef-coding-agent
version: 1.0.0
description: >
  The default Alef coding agent blueprint.
  This manifest is the SBOM — the adapter list is the canonical record
  of what ships. No code. Assembly is the runner's concern.

adapters:
  - name: fs
    package: "@dpopsuev/alef-tool-fs"
  - name: shell
    package: "@dpopsuev/alef-tool-shell"
    blockedPatterns:
      - "--no-verify"
      - "--no-gpg-sign"
      - "git\\s+reset\\s+--hard"
      - "git\\s+commit\\s+-n"
      - "HUSKY=0"
      - "rm\\s+-rf\\s+/"
  - name: web
    package: "@dpopsuev/alef-tool-web"
  - name: agent
    package: "@dpopsuev/alef-tool-agent"
  - name: skills
    package: "@dpopsuev/alef-tool-skills"
  - name: factory
    package: "@dpopsuev/alef-tool-factory"
  - name: plan
    package: "@dpopsuev/alef-tool-plan"
  - name: discourse
    package: "@dpopsuev/alef-tool-discourse"

capabilities:
  orchestration: true

memory:
  session: memory
