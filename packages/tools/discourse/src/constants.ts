/** Hard deadline for one external projection request. */
export const SCRIBE_REQUEST_TIMEOUT_MS = 10_000;
/** Maximum response payload accepted from the projection endpoint. */
export const SCRIBE_RESPONSE_MAX_BYTES = 1_048_576;
/** Maximum response detail retained in an HTTP error. */
export const SCRIBE_ERROR_DETAIL_MAX_CHARS = 200;
